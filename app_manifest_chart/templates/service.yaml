apiVersion: v1
kind: Service
metadata:
  labels:
    app: "{{ .Values.name }}"
  name: "{{ .Values.name }}"
spec:
  type: NodePort
  ports:
  - nodePort: 30088
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: "{{ .Values.name }}" 
